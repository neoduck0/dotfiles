`add` -> new group tracked by the repository
`remove` -> group no longer tracked by the repository
`clean` -> modify text files without behavior change
`fix` -> fix an issue
`alter` -> change behavior without fixing an issue
